import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: '100%',
        flexDirection: 'column',
        alignItems: 'center',
        margin: 10,
    },

    texto: {
        fontSize: 30,
        padding: 20
    },

    buttonContainer: {
        width: '100%', 
    }
})